<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<a class="navbar-brand" href="{{path('home')}}">Queen S'y Beauty</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarColor03">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item dropdown">
				<a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Catégories</a>
				<div class="dropdown-menu">
					{# {% for category in globalRender.findAllCategories %}
						<a class="dropdown-item" href="#">{{category.name}}</a>
					{% endfor %} #}
					{% for i in 1..7 %}
						<a class="dropdown-item" href="#">Categorie {{i}}</a>
					{% endfor %}
				</div>
			</li>
		</ul>
		<form class="form-inline my-2 my-lg-0 mr-auto" data-children-count="1">
			<input class="form-control mr-sm-2" type="text" placeholder="Produit, Categorie, ..." style="width: 30em;">
			<button class="btn btn-primary my-2 my-sm-0" type="submit">Rechercher</button>
		</form>
		<ul class="navbar-nav ml-auto">
			<li class="nav-item dropdown">
				<a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
					<button class="btn btn-primary">
						{% if app.user %}
						Bienvenue {{app.user.lName}}
						{% else %}
							Connexion
						{% endif %}
					</button>
				</a>
				<div class="dropdown-menu">
					{% if app.user %}
					<a class="dropdown-item dropdown-item-btn" href="{{path('app_logout')}}">
						<button class="btn btn-primary btn-block">Déconnexion</button>
					</a>
					{% else %}
					<a class="dropdown-item dropdown-item-btn " href="{{path('app_login')}}">
						<button class="btn btn-primary btn-block">Connexion</button>
					</a>
					<a class="dropdown-item dropdown-item-btn" href="{{path('app_register')}}">
						<button class="btn btn-primary btn-block">Inscription</button>
					</a>
					{% endif %}
					<div class="dropdown-divider"></div>
					<a class="dropdown-item" href="#">Mes Commandes</a>
					{# <a class="dropdown-item" href="#">Mes envies</a> #}
					<a class="dropdown-item" href="#">Mon compte</a>
				</div>
			</li>
		</ul>
		<ul class="navbar-nav ml-1">
			<li class="nav-item">
				<a class="nav-link" href="#">Qui somme-nous <i class="fas fa-question-circle"></i></a>
			</li>
			<li class="nav-item">
				{# <a class="nav-link" href="{{path('cart_show')}}"><i class="fas fa-shopping-basket"></i> Panier<spa class="font-weight-bold" >({{globalRender.getCartProductCount}})</span></a> #}
				<a class="nav-link" href="#"><i class="fas fa-shopping-basket"></i> Panier<spa class="font-weight-bold" >(2)</span></a>
			</li>
		</ul>
	</div>
</nav>
