{% set controller = app.request.attributes.get('_controller') %}
<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-5">
        <a class="navbar-brand" href="{{ path('admin_dashboard') }}">Queen S'y Beauty Admin</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03"
            aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
    
        <div class="collapse navbar-collapse" id="navbarColor03">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item {{active_by_route('admin_dashboard')}}">
                    <a class="nav-link" href="{{ path('admin_dashboard') }}">Dashboard</a>
                </li>
                <li class="nav-item {{active_by_controller(controller)}}">
                    <a class="nav-link" href="{{path('admin_category_index')}}">Catalogue</a>
                </li>
                {# <li class="nav-item ">
                    <a class="nav-link" href="{{path('admin_sub_category_index')}}">Sous-categories</a>
                </li> #}
                {# <li class="nav-item ">
                    <a class="nav-link" href="{{path('admin_product_index')}}">Articles</a>
                </li> #}
                <li class="nav-item {{active_by_route('admin_order_index')}}">
                    <a class="nav-link" href="{{path('admin_order_index')}}">Commandes</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a href="#" class="nav-link">Parametrès</a>
                </li>
                <li class="nav-item">
                    <a href="{{path('app_logout')}}" class="nav-link">Deconnexion</a>
                </li>
            </ul>
        </div>
    </nav>
</header>
