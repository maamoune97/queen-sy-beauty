{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <h1>Nouvelle arrivage</h1>
    <div class="row">
        {% for product in products %}
            {% include "partials/product_cart.html.twig" with {'product' : product } %}        
        {% endfor %}
    </div>
</div>
{% endblock %}
